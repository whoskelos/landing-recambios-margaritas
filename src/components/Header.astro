---

---

<header id="header-nav" class="fixed top-0 left-0 w-full bg-transparent z-50 [mask-image:linear-gradient(to_bottom,_black_80%,_rgba(0,_0,_0,_0.01)_100%)]">
    <nav
        class="w-full max-w-[90%] mx-auto flex justify-between items-center py-6"
    >
        <div>
            <span class="font-bold tracking-wider text-white text-2xl uppercase"
                ><strong class="text-red-500">Recambios</strong> las Margaritas</span
            >
        </div>
        <div>
            <ul
                class="text-white text-lg font-semibold flex items-center justify-center gap-x-4 [&>li>a]:p-2"
            >
                <li>
                    <a href="#inicio">INICIO</a>
                </li>
                <li>
                    <a href="#productos">PRODUCTOS</a>
                </li>
                <li>
                    <a href="#contacto">CONTACTO</a>
                </li>
            </ul>
        </div>
    </nav>
</header>

<style>
    a:hover {
        color: rgb(239, 68, 68);
        transition: color 0.3s;
    }
    #header-nav {
        animation: blur linear both;
        animation-timeline: scroll();
        animation-range: 100px;
    }

    @keyframes blur {
        to {
            box-shadow:
                0px 5px 50px -5px rgba(0, 0, 0, 0.1),
                0px 5px 50px rgba(0, 0, 0, 0.3);
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
        }
    }
</style>
